[project]
name = "wechatbridge"
version = "1.2.2"
description = "Bridge WeChat messages to agy or Grok Build CLIs — text/image/file/voice in, replies and selected artifacts back."
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "WeChatBridge contributors" },
]
dependencies = [
    "httpx",
    "qrcode",
    "Pillow",
    "cryptography",
]

[project.scripts]
wechatbridge = "wechatbridge.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["wechatbridge"]
